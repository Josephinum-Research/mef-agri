Model-Tree
==========

mef_agri.models.tree.py

This module contains the ``ModelTree`` class, which ensures, that models are 
connected and that all model-quantities are accessible from every location in 
the model-tree.

ModelTree class
---------------

.. autoclass:: mef_agri.models.tree.ModelTree
    :members: